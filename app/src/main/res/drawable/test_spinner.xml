<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 勾选的样式图放到res目录下的drawable中，然后在这里使用 -->

    <item android:drawable="@mipmap/choose2_pressed" android:state_checked="true" />
    <!-- 未勾选的样式图放到res目录下的drawable中，然后在这里使用 -->
    <item android:drawable="@mipmap/choose2_normal" android:state_checked="false"/>
    <item android:drawable="@mipmap/choose2_normal"/>

</selector>